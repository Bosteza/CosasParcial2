#include<stdio.h>

int main()
{

int numeros[5];
int i,n=5;

for (i=0;i<n;i++){
		printf("Escriba un numero:");
		scanf("%d",&numeros[i]);
		}

for(i=0;i<n;i++){ 
		numeros[i]*=2;
		printf("%d\n",numeros[i]);
		}
printf("\n");

return 0;
		
}
